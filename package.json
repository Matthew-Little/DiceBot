{
	"name": "dicebot",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "Matt L.",
	"type": "module",
	"main": "DiceBot.ts",
	"scripts": {
		"clean": "del /s /q build",
		"build": "tsc --build",
		"start": "npm run clean && npm run build && node src/DiceBot.ts",
		"startNoClean": "npm run build && node src/DiceBot.ts"
	},
	"dependencies": {
		"discord.js": "^14.23.2"
	},
	"devDependencies": {
		"@eslint/js": "^9.38.0",
		"@types/node": "^24.9.1"
	}
}